using Unity.Netcode;
using UnityEngine;

public class RoomTriggerController : NetworkBehaviour
{
    /*public RoomSystem roomSystem;
    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag("Player"))
        { 
            playerInRoom++;
            inRoom = true;
        }
    }
    private void OnTriggerExit(Collider other)
    {
        if (other.gameObject.CompareTag("Player"))
        {
            playerInRoom--;
            inRoom = false;
        }
    }*/
}
